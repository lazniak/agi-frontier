# Gold set — frozen human-curated seed

This is the dataset as researched by hand (Claude sessions, 2026-09-07) — 102 releases, 413
scores, every number with a source URL and a verbatim quote. **It is never published.** The site
shows `data/models/`, which the OpenRouter researcher fills; this directory is the answer key the
researcher is evaluated against (`worker eval`, REDESIGN §6). Edit only to fix a factual error,
and say so in the commit message.
